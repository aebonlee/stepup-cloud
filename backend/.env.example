# Backend Environment Variables
# Render 배포용 환경변수 설정 예시
# 실제 배포시에는 Render 대시보드에서 이 값들을 설정하세요

# 서버 포트 (Render에서 자동으로 설정됨)
PORT=5003

# JWT 시크릿 키 (보안을 위해 강력한 키로 변경)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# 프론트엔드 URL (CORS 설정용)
FRONTEND_URL=https://aebonlee.github.io

# 노드 환경
NODE_ENV=production

# 데이터베이스 URL
# PostgreSQL (Render): postgresql://user:password@host:port/database
# SQLite (개발환경): ./stepup_cloud.db
DATABASE_URL=postgresql://your-db-url
